{
	"host": "127.0.0.1",
	"port": "8080",
	"dbhost": "localhost",
	"database": "bluro",
	"user": "root",
	"password": "HardPass123",
	"connectTimeout": 10000,
	"modules": ["Auth", "Article", "Comment"],
	"cors": {
		"allowedMethods": ["PUT", "DELETE", "POST", "GET"],
		"allowedOrigins": ["http://localhost:3000"],
		"allowedHeaders": ["Content-Type", "Cookies"],
		"exposedHeaders": [],
		"permissionAge": 86400,
		"allowCredentials": true
	}
}
